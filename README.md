# 🚀 My Data Engineering & Programming Journey

Welcome to my central learning repository! This space documents my daily progress, coding scripts, database projects, and advanced analytical spreadsheets as I build my foundation to become a professional Data Engineer.

---

## 📁 Repository Architecture & Modules

### 🐍 Python Programming
* **`introduccion/`**: Foundational Python concepts, resource management (`gestor_resources`), and basic control flow (`control_flow`). Includes the main execution file (`main.py`).
* **`estructuras_control/`**: Intermediate logic implementations mixing loops, dictionaries, data processors, and status evaluations (`evaluar_estados`, `gestion_diccionarios.py`, `procesador_datos`).
* **`proyectos/`**: Practical applications and unifications of programming logic.

### 🛢️ SQL Database Engineering
* **`Modulo_02_Joins/`**: Relational database theory and complex unifications.
    * `02_reto_deptos_fantasmas.sql`: Query implementation for identifying orphaned or empty department records using advanced `LEFT JOIN` and `IS NULL` filters.

### 📊 Excel for Analytics & Data Integration
* **`excel/`**: Advanced calculation blueprints and relational spreadsheet architecture.
    * `financial_models_and_data_integration.xlsx`: Master workbook housing automated debt servicing models, mathematical aggregations, and multidimensional key lookups.

---

## 🛠️ Environment & Technical Stack
* **Languages & Query Languages:** Python 3.x, SQL (ANSI standard).
* **Spreadsheet Engineering:** Excel Web (English syntax / Semicolon `;` regional argument separators).
* **Database Engine:** SQLite (via DB Browser for SQLite).
* **Tools:** Visual Studio Code, Git, and GitHub for version control.

---

## 🧠 Core Competencies Applied in Excel
* **Relational Lookups (`XLOOKUP`):** Implemented scalable lookups using full-column vector references (`A:A`) to dynamically merge transactional event tables with dimensional master catalogs, bypassing `#N/A` errors with production-grade `If_Not_Found` handling.
* **Complex Control Flow (`IF`, `AND`, `OR`):** Built automated audit urgency flags and conditional tracking mechanisms by nesting multi-variable boolean operators within standard decision-making blocks.
* **Financial Modeling (`PMT`):** Designed a robust debt-amortization engine driven by the `PMT` function, verifying absolute formula precision by routing calculated payments down to an exact zero-ending balance sequence.

---
_“The goal is not just to write queries and scripts that work, but solutions that scale.”_